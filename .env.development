# Vite only exposes environment variables that start with VITE_
# the old REACT_APP_* names are incompatible and caused the runtime error
# "process is not defined" when we tried to read them in the browser.
VITE_BASE_URL=https://api.rowgaming669.com
VITE_API_VERSION=/api/v1